---
- name:
  fetch:
    src: /tmp/date.log
    dest: /tmp/
    flat: yes
  when: aaa == 1

- name: get output
  set_fact:
    datedate: "{{ lookup('file', '/tmp/date.log')|string|regex_search('^aa') }}"
    certificate_key: "1"
    discovery_tocken_ca_cert_hash: "2"
    token: "3"

